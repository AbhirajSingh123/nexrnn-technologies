// No static demo workshops — this content type starts empty until the admin
// creates real workshops in the admin panel. Kept for structural consistency
// with services.js / courses.js's fallback pattern.
export const WORKSHOPS = [];
